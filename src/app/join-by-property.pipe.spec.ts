import { JoinByPropertyPipe } from './join-by-property.pipe';

describe('JoinByPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinByPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
